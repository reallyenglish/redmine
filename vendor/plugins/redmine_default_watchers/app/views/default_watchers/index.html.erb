<h2>Default Watchers</h2>

<% form_tag({ :action => 'update' }) do %>

<%= submit_tag l(:button_submit) %>

<ul>
<% @all_active_users.each do |user| %>
<li>
<%= check_box_tag 'watcher_ids[]', user.id, @default_watchers.include?(user) %> <%=h user %></label>
</li>
<% end %>
</ul>

<% end %>
